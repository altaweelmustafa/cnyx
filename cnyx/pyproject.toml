[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cnyx"
version = "1.0.5"
description = "Search and download songs from the CLI"
dependencies = ["yt-dlp"]

[project.scripts]
cnyx = "cnyx:main"

[tool.setuptools]
py-modules = ["cnyx"]
